# ============================================================
# LLM Persona Cold-Start Forecasting — Python 의존성
# ============================================================
# 설치: pip install -r requirements.txt
# Python: 3.10+
#
# Track A (로컬) + 공통 패키지는 섹션 1-3
# Track B (연구실 서버) 전용 패키지는 섹션 4
# ============================================================

# ----------------------------------------------------------
# 1. 공통 데이터 처리
# ----------------------------------------------------------
numpy>=1.26.0
pandas>=2.1.0
scipy>=1.11.0
scikit-learn>=1.4.0

# ----------------------------------------------------------
# 2. 설정 및 유틸리티
# ----------------------------------------------------------
pyyaml>=6.0.1
tqdm>=4.66.0

# ----------------------------------------------------------
# 3. Track A: OpenAI API (GPT-4o-mini 예측, GPT-4o 페르소나 생성)
# ----------------------------------------------------------
openai>=1.35.0

# ----------------------------------------------------------
# 4. Track B: HuggingFace Qwen 2.5 32B (연구실 서버 전용)
#    로컬 설치 시 GPU 메모리 충분한 경우에만 사용
# ----------------------------------------------------------
torch>=2.3.0                        # CUDA 지원 버전은 별도 설치 권장
                                     # pip install torch --index-url https://download.pytorch.org/whl/cu121
transformers>=4.45.0                # Qwen2.5 지원 최소 버전
accelerate>=0.33.0                  # device_map="auto" 멀티-GPU 지원
bitsandbytes>=0.43.0                # INT4/INT8 양자화 (track_b.quantization.mode="4bit"|"8bit")
sentencepiece>=0.2.0                # Qwen 토크나이저 의존성
protobuf>=4.25.0                    # sentencepiece 의존성

# ----------------------------------------------------------
# 5. 개발 / 실험 편의
# ----------------------------------------------------------
matplotlib>=3.8.0                   # 결과 시각화
seaborn>=0.13.0
jupyter>=1.0.0                      # 노트북 분석

# ----------------------------------------------------------
# 설치 순서 권장 (연구실 서버)
# ----------------------------------------------------------
# 1) CUDA 버전 확인:  nvcc --version
# 2) PyTorch 설치:
#    pip install torch torchvision --index-url https://download.pytorch.org/whl/cu121
# 3) 나머지:
#    pip install -r requirements.txt --ignore-installed torch
# ----------------------------------------------------------
